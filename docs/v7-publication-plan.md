# V7 evidence publication plan

Status: candidate; context profile: artifact-write; audience: public; verified: false.

Source: direct user release to add the report and supporting artifacts and commit/push the repaired public-ready changes. Owner: Agentic Knowledge OS. Scope: the existing pending source, schemas, fixtures, documentation and tests from this release workflow, plus a reviewed v7 evidence projection. No sibling repositories, live profiles, credentials, provider runs, tags or separate release announcements are included.

1. Inspect Git state and the expected repository remote; preserve historical evidence.
2. Export only frozen source/data, protocol, plan, prompts, raw synthetic-task responses, transport metadata, assessments and result records. Exclude credentials, caches, machine logs and unrelated files. Preserve exact archived bytes and publish archive/file hashes; normalize only separately labeled readable summaries.
3. Replay the exported evidence offline and compare JSON results. Check public-safety markers, source tests, compilation, lifecycle checks, repository validation and whitespace.
4. Stage the reviewed release inventory explicitly, inspect the staged diff, commit, push without force, and verify remote HEAD. Stop on unexpected remote divergence or unrelated changes.

The result remains a twelve-task development comparison, not a general model baseline or proof of broader intelligence. Publication does not install a host, change license terms, accept semantics or set verified true.
