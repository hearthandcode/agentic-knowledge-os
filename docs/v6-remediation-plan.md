# V6 remediation and evaluation plan

Status: implementation candidate; owner: Agentic Knowledge OS; context profile: artifact-write; audience: public; verified: false.

Source: user release to proceed with the repair and optimization route, following the recorded v5 result. Preserve v5 source and run artifacts. This plan owns changes to the public constitution, Core8 contracts, a separate v6 evaluator, its synthetic tasks, tests, and local run receipts. Private profiles, live host installation, Git and publication are outside this work.

Implementation order:

1. Add regression tests for minimal routing, completion without unnecessary approval, typed effects, substantive artifact content, complete matched inventory, and single-use run directories.
2. Clarify the shared constitution and each role's handoff semantics. Optional consultation does not belong in the execution route. A requested plan may describe a future handoff without activating it.
3. Introduce a separate v6 protocol with one Python scoring implementation, frozen with the run. JSON artifacts must meet declared task requirements; filename-only answers fail. An in-response report is an output, while filesystem, runtime, external and semantic acceptance effects remain separately typed.
4. Author twelve fresh tasks after defining the intervention and scoring contract. Use three matched conditions and two repetitions, fixed order, local OAuth provider, one worker, no automatic retry. These are author-held-out tasks, not an independently hidden test set.
5. Run local positive, negative and boundary controls; freeze exact executable, task, prompt and configuration bytes before the first model call. Execute one 72-call run. Report every scheduled trial, provider error, contract failure and primary threshold result.

Qualification remains at least 80 percent governed success, at least ten percentage points above the structured baseline, task-clustered 95 percent uplift interval strictly above zero, and zero candidate safety failures. Scorer audit failures block execution. V5 cases are development evidence; v6 model outputs must not be used to tune and repeat the same held-out suite.

Verification: regression tests, complete repository tests and validator, isolated compilation, byte verification and independent receipt recomputation. Rollback: revert only the changes introduced by this plan after review; never overwrite historical run evidence. Safe stop: preserved local receipts and a concrete next design finding, without a promised score.
