# Public release checklist

## GitHub repository

- Owner: `hearthandcode`
- Repository: `agentic-knowledge-os`
- URL: `https://github.com/hearthandcode/agentic-knowledge-os`
- Visibility: confirm directly in GitHub before announcement
- Default branch: `main`
- Description: `A local-first, governed operating system for user-owned extended minds across agent harnesses.`
- Topics: `agentic-ai`, `knowledge-management`, `hermes-agent`, `ai-agents`, `local-first`, `provenance`, `human-in-the-loop`

This name matches the Python package and local repository and sits alongside `hearthandcode/hermes-knowledge-engineering-plugins` without implying that Hermes owns the portable system. The repository was created by the human owner, and local Git currently records baseline commit `42a31c9` on `main` aligned with `origin/main`. Remote visibility, rendered contents, and release state must still be observed directly before claiming publication.

## Content and rights review

- confirm the copyright owner and authorship chain;
- verify the selected PolyForm/CC license split is represented consistently in `LICENSE`, package metadata, documentation, and notices;
- obtain qualified legal review if patent, trade-secret, or commercial licensing value may be material;
- inspect every proposed tracked file for private paths, internal identifiers, credentials, personal data, unpublished excerpts, and unsupported claims;
- confirm that public Core8 wording is original adaptation rather than copied private source;
- adopt a reserved-marks policy if desired;
- keep outside contributions closed until contributor terms are selected.

## Release sequence

Initial release sequence:

1. create `hearthandcode/agentic-knowledge-os` — human-reported complete;
2. create and push the baseline source commit — locally observed at `42a31c9` on `main` and `origin/main`;
3. review the host-package implementation and updated release documentation in the current working tree;
4. rerun the provider-free test suite, repository validator, package generation, package-manifest verification, and diff checks;
5. create and push the reviewed release commit only under a separate Git authorization;
6. inspect GitHub-rendered files, repository metadata, license detection, file inventory, and visibility;
7. publish `v0.3.0-alpha.1` as a prerelease, not a production release, under a separate release authorization;
8. confirm the public URL and only then use the announcement copy.

The baseline push exists. This documentation update does not authorize staging, committing, pushing, changing visibility, creating a tag or GitHub release, or posting the community announcement.

## Candidate contents

- provider-free workspace lifecycle evaluation;
- public Core8 fleet and RFC-style extended-mind constitution;
- deterministic native Hermes Agent Plugins v1 package generation;
- deterministic native Pi skill and prompt package generation;
- exact package planning, rendering, writing, and digest verification;
- PolyForm Noncommercial software license, CC BY-NC-SA documentation license, notices, and reserved marks;
- explicit holds for live profile activation, provider use, credentials, publication, and the Exocore bridge.

## Public-claim checks

- use `source-available`, not `open-source`, if a noncommercial restriction is selected;
- call this an early design or alpha candidate;
- distinguish generated host-package compatibility checks from live profile activation or model-session behavior;
- identify the exact observed checks when useful: Hermes Agent v0.21.0 package-doctor discovery and Pi v0.83.0 isolated local installation;
- disclose AI assistance where appropriate;
- invite review of boundaries and portability rather than claiming intelligence, safety, or efficacy.
