# Changelog

All notable changes to Agentic Knowledge OS will be recorded here.

## Unreleased

- add the public v7 evidence projection (191 archived files), offline replay verifier and tamper-rejection tests; all 99 tests pass, with no additional provider calls.

- implement and freeze the v7 compact-contract comparison with 36 matched trials, at most 72 calls, equal gate feedback, independent content scoring, raw-response replay and a single-use launch guard;
- record the completed 38-call development run: compact and constitution-only each 11/12 post-repair task successes, baseline 6/12; preserve the exact-match and small-sample limitations and historical v5/v6 results.

- add an optional compact artifact contract, single-role prompt compilation, closed consumer-schema and source-value enforcement, CLI checks and bounded repair receipts; preserve historical benchmark scores and make no new behavioral-effectiveness claim.

- clarify minimal handoffs, optional consultation, completed reports of unresolved evidence, and substantive candidate outputs across the constitution and Core8;
- add a separate v6 scorer with concrete JSON artifact checks, typed consequential effects, complete matched inventory, task-clustered uncertainty and single-use frozen run directories;
- retain the existing qualification thresholds and preserve v5 historical evidence; v6 uses a new task and scoring protocol and must not be presented as a directly comparable score increase.
- record the complete v6 OAuth result: full AKOS 54.17/100, constitution only 50/100, structured baseline 41.67/100; zero candidate hard gates, but the minimum score and confidence interval requirements failed.

- add a frozen v5 remediation benchmark with bounded upstream completion, typed source dispositions, exact primary-plus-ordered-handoff routing, and candidate-versus-effect separation;
- move MiniMax OAuth prompt transport from process arguments to Promptfoo Python-worker IPC plus `mmx` standard input, preventing full-AKOS prompts from exceeding the operating-system argument limit;
- record the first complete 72-output v5 run as `not-qualified`: AKOS 12.5/100 versus structured baseline 0/100, uplift interval 0 to 29.17 points, and one candidate hard-gate classification;
- preserve the v5 result as negative evidence, including over-routing, excess human-decision holds, and the performed-effect wording ambiguity, without post-hoc threshold or case changes;
- add a v4 publication-candidate benchmark with twelve new held-out tasks, two repetitions, three matched conditions, and 72 expected MiniMax-M3 calls;
- define Governed Task Success Rate as a non-compensating conjunctive primary endpoint, with task-clustered uncertainty and frozen publication thresholds;
- separate output-integrity failures from safety hard gates, make candidate safety gates control candidate eligibility, and preserve baseline failures as comparative evidence;
- score routing against preregistered acceptable profile sets and candidate contract adherence against the selected valid profile.

- added a framework-neutral, eight-case Core8 governance benchmark;
- added adapter-neutral trace validation and five-axis policy-conformance scoring;
- made semantic acceptance, unauthorized effects, private-source disclosure, profile-state borrowing, and silent write-back hard gates;
- added deterministic byte-identity ledgers with an explicit identity-only claim boundary;
- projected the benchmark into generated Hermes and Pi packages;
- added CLI scoring, provider-free positive and negative canaries, schemas, fixtures, tests, and evaluation guidance.
- added a 13-probe mutation audit covering every governance axis, hard gate, inventory closure, and byte drift;
- added a preregistered three-condition behavioral experiment with repeated calibration and held-out tasks;
- added paired per-metric deltas, win/tie/loss counts, deterministic bootstrap intervals, and a prohibition on compensating composite scores;
- added behavioral plan, observations, receipts, CLI commands, generated package references, schemas, and synthetic canaries.
- added an optional, secret-free Promptfoo adapter for a frozen MiniMax-M3 matched comparison;
- added separate API-key and official `mmx` OAuth routes, explicit live-provider confirmation, isolated local Promptfoo state, and disabled sharing, telemetry, and remote generation;
- added deterministic Promptfoo assertions and normalization into AKOS observations while holding correction and recovery metrics when their evidence is absent.
- strengthened every public Core8 profile with a closed contract version, four RFC rules, named failure behavior, and evidence checks;
- expanded the extended-mind constitution with profile admission and adversarial instruction-integrity clauses;
- expanded the matched comparison to 72 rows across eight calibration and held-out adversarial tasks, one per Core8 role;
- removed expected-route and outcome-criterion disclosure from model prompts and added independent profile-routing, contract-adherence, and adversarial-resistance metrics.
- preserve malformed or open runner outputs as `contract_violation` hard gates with fail-closed scores instead of aborting or dropping the matched run.
- preserve the v2 plan and canary while adding a v3 36-call design with twelve matched tasks, equal condition assertions, eight calibration cases, and four newly authored held-out cases;
- split detected task risks from violations committed by a response, admit `refused` effect dispositions, and score task-aware considered/admitted/excluded source handling;
- mark profile-contract adherence not applicable for baselines and freeze the v3 scorer source identity before provider execution.

## 0.3.0-alpha.1 — 2026-09-05

Initial public alpha release candidate.

### Included

- deterministic `brain.json` planning and in-memory bundle rendering;
- public Core8 functional profile distribution;
- RFC-style extended-mind `AGENTS.md` constitution;
- closed type kernel and machine-readable operating policy;
- neutral, Hermes, Pi, and held Exocore adapter descriptors;
- deterministic Hermes Agent Plugins v1 package generation;
- deterministic Pi skill and prompt package generation;
- exact-confirmation package planning, rendering, writing, and digest verification;
- isolated package compatibility checks against Hermes Agent v0.21.0 and Pi v0.83.0;
- exact-confirmation local apply with an ownership manifest;
- byte-drift verification, rollback, and uninstall boundaries;
- provider-free alpha lifecycle evaluation;
- schemas, fixtures, tests, public-safety checks, and licensing boundaries.

### Held

- live Hermes and Pi profile registration or activation;
- Exocore runtime bridging;
- provider, model, credential, network, or hosted-service integration;
- automatic semantic acceptance or memory write-back;
- production-readiness, usefulness, safety, or compatibility claims.
